###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, Space, exec, $menu # App launcher
bind = $mainMod, Return, exec, $terminal # Terminal
bind = $mainMod SHIFT, Return, exec, $browser # Browser
bind = $mainMod, E, exec, $fileManager # File manager
bind = $mainMod, Escape, exec, swaync-client -t -sw # Control/Notification center
bind = $mainMod, L, exec, hyprlock # Lock system


# Extra
bind = $mainMod, H, exec, ~/.config/rofi/scripts/key-help.sh # Show key bindings
bind = , Print, exec, hyprshot -m output -m "$(hyprctl activeworkspace -j | jq -r '.monitor')" # Screenshot (fullscreen)
bind = SHIFT, Print, exec, pgrep slurp || hyprshot -m region # Screenshot (region)
bind = SUPER, V, exec, ~/.config/rofi/scripts/clipboard.sh # Clipboard
bind = $mainMod, W, exec, pgrep waypaper || waypaper # Select wallpaper

# System controls
bind = CTRL SHIFT, ESCAPE, exec, kitty --class btop-float btop # System monitor
bind = $mainMod SHIFT, A, exec, kitty --class wiremix-float wiremix # Audio controls
bind = $mainMod SHIFT, B, exec, kitty --class bluetui-float bluetui # Bluetooth controls
bind = $mainMod SHIFT, W, exec, kitty --class impala-float sudo impala # Wifi controls

# Window
bind = $mainMod, Q, killactive, # Close window
bind = $mainMod, J, layoutmsg, togglesplit # Toggle window split
bind = $mainMod SHIFT, V, togglefloating, # Toggle window floating/tiling
bind = $mainMod, P, pseudo, # Pseudo window
bind = $mainMod, F, fullscreen, 0 # Fullscreen
bind = $mainMod SHIFT, F, fullscreen, 1 # Maximize window

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l # Move window focus left
bind = $mainMod, right, movefocus, r # Move window focus right
bind = $mainMod, up, movefocus, u # Move window focus up
bind = $mainMod, down, movefocus, d # Move window focus down

# Swap active active window
bind = $mainMod SHIFT, left, swapwindow, l # Swap window to the left
bind = $mainMod SHIFT, right, swapwindow, r # Swap window to the right
bind = $mainMod SHIFT, up, swapwindow, u # Swap window up
bind = $mainMod SHIFT, down, swapwindow, d # Swap window down

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1 # Switch to workspace 1
bind = $mainMod, 2, workspace, 2 # Switch to workspace 2
bind = $mainMod, 3, workspace, 3 # Switch to workspace 3
bind = $mainMod, 4, workspace, 4 # Switch to workspace 4
bind = $mainMod, 5, workspace, 5 # Switch to workspace 5
bind = $mainMod, 6, workspace, 6 # Switch to workspace 6
bind = $mainMod, 7, workspace, 7 # Switch to workspace 7
bind = $mainMod, 8, workspace, 8 # Switch to workspace 8
bind = $mainMod, 9, workspace, 9 # Switch to workspace 9
bind = $mainMod, 0, workspace, 10 # Switch to workspace 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1 # Move window to workspace 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2 # Move window to workspace 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3 # Move window to workspace 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4 # Move window to workspace 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5 # Move window to workspace 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6 # Move window to workspace 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7 # Move window to workspace 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8 # Move window to workspace 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9 # Move window to workspace 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 # Move window to workspace 10

# Silently move active window to a workspace with mainMod + CTRL + [0-9]
bind = $mainMod CTRL, 1, movetoworkspacesilent, 1 # Silently move window to workspace 1
bind = $mainMod CTRL, 2, movetoworkspacesilent, 2 # Silently move window to workspace 2
bind = $mainMod CTRL, 3, movetoworkspacesilent, 3 # Silently move window to workspace 3
bind = $mainMod CTRL, 4, movetoworkspacesilent, 4 # Silently move window to workspace 4
bind = $mainMod CTRL, 5, movetoworkspacesilent, 5 # Silently move window to workspace 5
bind = $mainMod CTRL, 6, movetoworkspacesilent, 6 # Silently move window to workspace 6
bind = $mainMod CTRL, 7, movetoworkspacesilent, 7 # Silently move window to workspace 7
bind = $mainMod CTRL, 8, movetoworkspacesilent, 8 # Silently move window to workspace 8
bind = $mainMod CTRL, 9, movetoworkspacesilent, 9 # Silently move window to workspace 9
bind = $mainMod CTRL, 0, movetoworkspacesilent, 10 # Silently move window to workspace 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic # Switch to scratchpad
bind = $mainMod SHIFT, S, movetoworkspace, special:magic # Move window to scratchpad
bind = $mainMod CTRL, S, movetoworkspacesilent, special:magic # Silently move window to scratchpad

# Troubleshooting
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit # Terminate Hyprland session
bind = $mainMod, R, exec, killall -9 waybar && waybar & # Reload waybar

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
